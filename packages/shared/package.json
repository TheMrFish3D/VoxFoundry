{
  "name": "@voxfoundry/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types, utilities, and constants for VoxFoundry",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint . --ext .ts --ignore-path ../../.gitignore",
    "type-check": "tsc --noEmit",
    "test": "echo \"No tests yet for @voxfoundry/shared\"",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}